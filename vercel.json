{
  "buildCommand": "flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "if [ -d flutter ]; then git -C flutter pull; else git clone -b stable --depth 1 https://github.com/flutter/flutter.git; fi && flutter/bin/flutter config --enable-web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
